@model IEnumerable<Inmobiliaria_.Net_Core.Models.Propietario>

@{
    ViewData["Title"] = "Lista de Propietarios";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Lista de Propietarios</h2>
    <a asp-action="Crear" class="btn btn-primary">
        <i class="fas fa-plus"></i> Nuevo Propietario
    </a>
</div>

@if (TempData["Mensaje"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["Mensaje"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="card">
    <div class="card-body">
        @if (Model != null && Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Id</th>
                            <th>DNI</th>
                            <th>Nombre Completo</th>
                            <th>Email</th>
                            <th>Teléfono</th>
                            <th width="200">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var propietario in Model)
                        {
                            <tr>
                                <td>@propietario.Id</td>
                                <td>@propietario.Dni</td>
                                <td>@propietario.Apellido, @propietario.Nombre</td>
                                <td>@(propietario.Email ?? "No especificado")</td>
                                <td>@(propietario.Telefono ?? "No especificado")</td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a asp-action="Detalles" asp-route-id="@propietario.Id" 
                                           class="btn btn-info btn-sm" title="Ver detalles">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <a asp-action="Editar" asp-route-id="@propietario.Id" 
                                           class="btn btn-warning btn-sm" title="Editar">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <a asp-action="Eliminar" asp-route-id="@propietario.Id" 
                                           class="btn btn-danger btn-sm" title="Eliminar">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center py-5">
                <i class="fas fa-users fa-3x text-muted mb-3"></i>
                <h5 class="text-muted">No hay propietarios registrados</h5>
                <p class="text-muted">Comienza creando tu primer propietario</p>
                <a asp-action="Crear" class="btn btn-primary">
                    <i class="fas fa-plus"></i> Crear Propietario
                </a>
            </div>
        }
    </div>
</div>